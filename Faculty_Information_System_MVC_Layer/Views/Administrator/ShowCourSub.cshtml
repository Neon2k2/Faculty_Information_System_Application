@model IEnumerable<Faculty_Information_System_MVC_Layer.Models.CourseSubjectVM>



@{
    ViewData["Title"] = "ShowCourSub";
}

<link rel="stylesheet" href="~/Style/StyleSheet3.css" />



<p>
    <button class="rounded-left btn-dark"><a asp-action="CourSubcreate" class="text-decoration-none text-white">Add New Entry</a></button>
<br/></p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CourseSubjectId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubjectId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourseSubjectId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubjectId)
            </td>
            <td>
                @Html.ActionLink("Update", "CourSubEdit", new { id=item.CourseSubjectId }) |

                @Html.ActionLink("Delete", "CourSubDelete", new {  id=item.CourseSubjectId })
            </td>
        </tr>
}
    </tbody>
</table>

<div>
    <p>
    <button class="rounded-left btn-dark"><a asp-action="Index" class="text-decoration-none text-white">Back to Home</a></button>
<br/></p>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
